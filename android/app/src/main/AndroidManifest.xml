<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Schema personalizzato usato da Dtrek (web) per rilevare/aprire Navigator se
                 installato (lib/navigatorHandoff.ts) — un tentativo di redirect a
                 dtreknavigator://open che, se nessuna app lo gestisce, il browser lascia
                 semplicemente fallire in silenzio (nessuna finestra di errore su Android),
                 permettendo il fallback al navigatore via web. Sempre riapre semplicemente la
                 Home di Navigator (server.url in capacitor.config.ts) — non instrada verso una
                 schermata specifica, vedi commento in lib/navigatorHandoff.ts. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dtreknavigator" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Native Location Engine (spec §1/§13): foreground service so GPS
             acquisition, track recording and off-route detection keep running
             with the screen off, the app backgrounded, or the WebView paused —
             none of which a plain navigator.geolocation.watchPosition() survives. -->
        <service
            android:name=".nativelocation.LocationForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Normal (auto-granted) permission — needed by MainActivity's connectivity check
         (ConnectivityManager.getNetworkCapabilities) that gates the first-launch offline screen. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Continued tracking with the app backgrounded (spec §1). Google Play requires a
         prominent in-app disclosure + a separate user-initiated request for this permission
         before submission — see docs/navigation-engine-roadmap.md Phase 1 notes. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Android 13+ requires runtime permission to show the mandatory foreground-service notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
